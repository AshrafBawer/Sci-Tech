
// normally this folder will not be commited to github especially if it contains real creds

const config = {
    email : {
        name : "example@gmail.com",
        pass : 'password'
    }
};

module.exports = config;